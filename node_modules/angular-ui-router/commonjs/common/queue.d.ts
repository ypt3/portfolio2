/** @module common */ /** for typedoc */
export declare class Queue<T> {
    private _items;
    constructor(_items?: T[]);
    enqueue(item: T): T;
    dequeue(): T;
    clear(): Array<T>;
    size(): number;
    remove(item: T): T;
    peekTail(): T;
    peekHead(): T;
}
